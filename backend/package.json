{
  "name": "maestrohudapi",
  "version": "1.0.0",
  "description": "Backend code to serve up parsed procedure files.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "devstart": "nodemon app.js",
    "coverage": "nyc --reporter=lcov --reporter=text-summary ava",
    "test": "nyc --reporter=lcov --reporter=text-summary --report-dir=.coverage mocha --exit "
  },
  "dependencies": {
    "app-root-path": "^3.0.0",
    "express": "^4.17.1",
    "js-yaml": "^3.13.1",
    "mocha-lcov-reporter": "^1.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.0",
    "nodemon": "^2.0.2",
    "nyc": "^15.0.1",
    "request": "^2.88.2",
    "supertest": "^4.0.2"
  },
  "author": "",
  "license": "ISC"
}
